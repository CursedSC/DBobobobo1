-- LOTM Items Spawnmenu - ОТКЛЮЧЕНО
-- Вкладка отключена - используйте lotm_items команду

if not CLIENT then return end

-- Не регистрируем вкладку в Q-меню
-- Все предметы доступны через LOTM.UnifiedMenu.Open() или lotm_items

MsgC(Color(100, 255, 100), "[LOTM] ", Color(255, 255, 255), "Items spawnmenu disabled (use lotm_items)\n")
